const salaJS = ["Evaldo", "Camis", "Mari"];
const salaPython = ["Ju", "Leo", "Raquel"];

const salaUnificada = salaJS.concat(salaPython);

// Mudando a ordem do Array

const salaDev = salaPython.concat(salaJS);

console.log(salaUnificada);
console.log(salaDev);
