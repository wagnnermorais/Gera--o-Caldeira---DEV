const gradesButton = document.querySelector("#calcular");
const resultado = document.querySelector(".resultado");

gradesButton.addEventListener("click", (evento) => {
  resultado.innerHTML = "Fui clicado!";
});
